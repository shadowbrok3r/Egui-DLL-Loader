#[derive(Debug, Clone, Default)]
pub struct HollowDiagnostics {
    pub base_address_before: usize,
    pub base_address_after: usize,
    pub entry_point_rva_before: u32,
    pub entry_point_rva_after: u32,
    pub rip_before: u64,
    pub rip_after: u64,
    pub rsp_before: u64,
    pub rsp_after: u64,
    pub rbp_before: u64,
    pub rbp_after: u64,
    pub tls_rva_before: u32,
    pub tls_rva_after: u32,
    pub tls_size_before: u32,
    pub tls_size_after: u32,
    pub reloc_rva_before: u32,
    pub reloc_rva_after: u32,
    pub reloc_size_before: u32,
    pub reloc_size_after: u32,
    pub sections_before: Option<Vec<String>>,
    pub sections_after: Option<Vec<String>>,
    pub imports_before: Option<Vec<String>>,
    pub imports_after: Option<Vec<String>>,
    pub exports_before: Option<Vec<String>>,
    pub exports_after: Option<Vec<String>>,
    pub tls_callbacks_before: Option<Vec<usize>>,
    pub tls_callbacks_after: Option<Vec<usize>>,
    pub reloc_blocks_before: Option<Vec<usize>>,
    pub reloc_blocks_after: Option<Vec<usize>>,
}
